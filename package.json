{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "pug": "pug src/pug/public --out dist --pretty --watch",
    "pug-build": "pug src/pug/public --out dist --pretty",
    "sass": "sass src/sass:dist/styles --watch",
    "sass-build": "sass src/sass:dist/styles",
    "build": "npm run pug-build && npm run sass-build",
    "dev": "npm run pug && npm run sass"
  },
  "author": "Ciro Mora",
  "license": "MIT",
  "devDependencies": {
    "jstransformer-markdown-it": "^2.1.0",
    "pug": "^2.0.4",
    "pug-cli": "^1.0.0-alpha6",
    "sass": "^1.22.12"
  },
  "dependencies": {
    "highlight.js": "^10.1.2"
  }
}
